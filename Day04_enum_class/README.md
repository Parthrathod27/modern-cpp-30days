# Day 4 — enum class (Modern C++)

## 📌 Topic
`enum class` introduces **strongly typed enums** in Modern C++.

## 🎯 Why enum class?
- Prevents implicit conversion to int
- Avoids name conflicts
- Makes APIs safer and more readable

## 🧠 What this code explains
- Difference between old enum and enum class
- Scoped enum values (`OrderType::Buy`)
- Type-safe function parameters

## 📂 File
- `Day04_enum_class_explained.cpp`

## ✅ Key Takeaway
enum class helps write **clean, safe, and bug-resistant code**, especially in large systems.
